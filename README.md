# Sports Day Captions

- Use Node LTS >=14
- Set the `GOOGLE_APPLICATION_CREDENTIALS` environment variable to the absolute file path of a service account with Firestore and Cloud Speech to Text permissions.
- Run `tsc` to build
- Run `node dist/index.js`

Uses your default input microphone, and logs transcripts to console.

Fast internet recommended.
